# ── Web Framework ────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0          # [standard] includes websockets + httptools

# ── Vector Database ──────────────────────────────────────────
qdrant-client[fastembed]>=1.14.0   # [fastembed] pulls in fastembed automatically

# ── Embedding Model (FastEmbed) ──────────────────────────────
fastembed>=0.4.0                   # BAAI/bge-small-en-v1.5 model support

# ── Google Gemini LLM ────────────────────────────────────────
google-genai>=1.0.0                # google.genai + google.genai.types

# ── Data Validation & Settings ──────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0           # BaseSettings + SettingsConfigDict (app/config.py)

# ── Environment Variables ────────────────────────────────────
python-dotenv>=1.0.0               # .env file loading

# ── Retry Logic ─────────────────────────────────────────────
tenacity>=8.3.0                    # @retry decorator in llm_service.py

# ── HTTP Client (transitive — used by google-genai & qdrant) ─
httpx>=0.27.0

# ── Async Support ────────────────────────────────────────────
anyio>=4.4.0

#fastapi
#uvicorn
#qdrant-client
#google-genai
#pydantic
#pydantic-settings
#python-dotenv
#fastembed
#tenacity